.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store
# R's default graphics device output, dropped in the working dir when a plot is
# drawn non-interactively without an explicit device (e.g. a bare/printed ggplot
# in a reference script). A junk artifact, never shipped.
Rplots.pdf

# Ignore renv files that should not be in Git
renv/library/
renv/host-info.json
renv/state.json




# Internal archive of the pre-reduction clinical table (60/62 columns), kept only as a
# record. Nothing reads it, it is NOT covered by the 2026-08-19 release clearance, and it
# must never ship. The released tables live in released_data/. See restricted_data/README.md.
restricted_data/

# Large derived cache (brms fits, MaAsLin2/DESeq output) - regenerated, not shipped
intermediate_data/
data/
clean_Dai_text_and_figures_*.pdf
# Internal-only docs: their own private git repo, symlinked into the project root.
# Never ship. See internal/README.md.
internal/
CLAUDE.md
BUILD_LEDGER.md
HANDOFF*.md
/restricted_data

# Reference figure kept locally for orientation, not part of the reproduction
Study design and data flow.pdf
